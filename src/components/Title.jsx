import React from 'react'

const Title = props => {
    return (
        <div className='title'>
            {
                props.children
            }
        </div>

    )
}

export default Title