import React from 'react'
import PropTypes from 'prop-types'

const CourseRegisterResult = props => {
    
    return (
        <div>

        </div>
    )
}

CourseRegisterResult.propTypes = {
    type: PropTypes.string.isRequired,
    data: PropTypes.array
}

export default CourseRegisterResult
